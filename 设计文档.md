# Planning 策略、MCP 工具链路与异常处理设计

## 1. 设计目标

本项目面向本地生活短时出行规划，核心不是做 POI 搜索列表，而是把用户的一句话需求转成可执行行程。我们的取舍是：不让智能体在每次工具调用后开放式“自我反思”，而是基于 MCP 工具返回的事实，制定一套稳定的 planning 策略。这样可以发挥大模型擅长理解语义、组合方案的优势，同时把位置、距离、时间窗口、餐点、POI 真实性等硬约束交给代码侧规则处理，减少幻觉和不可控重试。

整体链路为：

```text
Intent -> Constraint Build -> Fact Gathering(MCP) -> Candidate Planning -> POI Detail Sync -> Rule Validation -> Repair Loop -> Scoring -> Final Plan -> Presentation -> Confirmation
```

其中 Planning 是主干：先将用户需求收敛成时间、地点、人数、活动类型、餐饮偏好、搜索半径等结构化约束；再调用高德 MCP 获取事实候选；然后生成 3 个候选方案；最后用规则校验和评分选出最终方案。模型不直接编造地点，也不直接决定方案是否合法。

## 2. Planning 策略

Planning 采用“事实池 + 策略组合 + 规则裁决”的方式。

第一步是约束收敛。`Constraint Build` 将自然语言意图转成 `plan_context`，包括出发地、开始/结束时间、是否亲子、是否需要活动/餐厅、活动关键词、餐厅关键词、距离偏好和搜索半径。比如近距离或亲子场景会自动收紧交通阈值，并优先使用较小半径搜索。

第二步是事实采集。`Fact Gathering` 不让模型自己决定去哪查，而是按固定顺序调用 MCP 工具：先地理编码，再天气，再活动/餐厅搜索，再补 POI 详情和通勤时间。候选池采用多关键词轮询收集，避免一个高频关键词挤占全部结果；活动和餐厅分别建池，餐厅使用高德餐饮类型过滤。

第三步是候选生成。`Candidate Planning` 让模型只在真实候选池内生成 3 个结构化方案，并要求输出 `poi_id`、阶段、活动时长和理由。代码侧再统一计算时间线：餐点窗口、通勤 ETA、活动时长上下限、结束时间容忍度均由求解器处理。也就是说，模型负责“语义上怎么搭配”，代码负责“物理上能不能走”。

第四步是校验与修复。`Rule Validation` 检查时间重叠、午餐/晚餐缺失、餐点过晚、超出结束时间等硬约束。若合法方案不足，则 `Repair Loop` 把失败原因转成明确修复指令，例如“压缩下午最后一个活动，使晚餐能在 19:00 前开始”，再回到候选生成。正常模式下至少保留 3 个合法方案才进入评分；最多修复 3 轮，防止无限循环。

第五步是评分选优。`Scoring` 综合首个活动 ETA、总通勤时间和用户需求匹配度排序；`Final Plan` 只从通过规则校验的方案里选最高分方案。高分不能覆盖硬约束失败，避免“看起来合适但实际不可执行”的结果。

## 3. MCP Tools 调用链路

高德能力统一封装为 `AmapMCPClient`，上层通过缓存客户端调用。主要 MCP tools 链路如下：

```text
maps_geo(origin_area)
  -> 得到 city / coordinates

maps_weather(city)
  -> 得到天气事实，用于室内/室外倾向

maps_around_search(keyword, location, radius)
  -> 有出发坐标时，按半径搜索活动、餐厅、途径点

maps_text_search(keyword, city, types)
  -> 无坐标或半径内无结果时，退化为城市范围搜索

maps_search_detail(poi_id)
  -> 补齐地址、坐标、电话、商圈、评分等详情

maps_distance(origin, destination, type=1)
  -> 计算驾驶距离和 ETA，用于排序、校验和提示

maps_direction_walking / maps_direction_driving / maps_direction_transit_integrated
  -> 可扩展为详细路线说明，不参与当前核心选点决策
```

调用策略是确定性的：先确定出发坐标，再围绕坐标检索；能用周边搜索就不先做全城搜索；每个关键词限量取数并去重；详情与距离按需补齐。缓存层按 geocode、weather、search、detail、distance 设置不同 TTL，减少重复调用和额度消耗。

## 4. 异常处理机制

已实现的关键异常是 POI 幻觉过滤。模型输出候选方案后，系统会检查每个 `poi_id` 是否存在于活动池、餐厅池中；不存在则判定为幻觉 POI，直接丢弃该 step，并记录 warning。若模型把餐厅误标成活动或把活动误标成餐厅，系统会根据真实候选池自动修正 `poi_type`。因此最终方案只能引用 MCP 或缓存返回过的真实 POI。

搜索范围异常采用半径分层处理。高德周边搜索依赖 `radius` 参数，半径外地点不会返回。系统默认按近距离/中距离使用不同半径；若半径内候选不足，可提示用户“当前位置 10km 内未找到足够匹配结果，是否扩大搜索或改为城市范围推荐”。若扩大后候选明显远离出发地，则在方案中标注“距离较远、通勤成本较高”，并询问用户是否继续前往。

网络连接或高德 API 不可用时，缓存层优先读取本地缓存；若实时 API 调用失败，则尝试从 `poi_detail_cache` 中按关键词回退出可用 POI，并在内部错误列表记录失败原因。若缓存也无匹配结果，系统不伪造地点，而是返回“附近暂无可用结果/建议调整关键词或扩大范围”的提示。

API 额度用尽或限流时，处理方式与网络异常一致：先命中 TTL 内缓存，再回退到本地 POI 详情缓存；仍失败则降级提示用户当前地图服务暂不可用。对于天气、距离等非核心字段缺失，Planning 会保守处理；对于活动/餐厅候选池为空这类核心事实缺失，则不进入执行确认。

时间与行程冲突由规则层处理。若候选方案出现午餐过晚、晚餐过晚、活动结束超过用户结束时间、相邻 step 时间重叠等问题，系统不会让模型自行解释，而是生成结构化修复指令并重规划。这样异常处理是可追踪、可复现的，而不是依赖智能体自由反思。

## 5. 设计总结

本方案的重点是“策略化 Planning”。我们认为 MCP 工具已经能提供足够明确的事实边界，因此没有采用让智能体反复反思工具调用的模式，而是把调用顺序、候选池构建、POI 白名单、时间求解、硬约束校验和重规划条件固定下来。模型只在受控空间内做语义组合，系统用规则保证可执行性，用 MCP 事实保证真实性，用评分策略保证推荐质量。
